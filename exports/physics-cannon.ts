/**
 * @hidden
 */

import '../cocos/physics/cannon/worker/wrapper';
import '../cocos/physics/cannon/instantiate';

// import CANNON from '@cocos/cannon';
// if (window) window.CANNON = CANNON;